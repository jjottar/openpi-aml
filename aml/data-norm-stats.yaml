$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
command: uv run compute_norm_stats.py --config-name ${{inputs.config_name}} --output-path-str ${{outputs.stats_folder}}
inputs:
  config_name: pi0_fast_libero
environment: azureml:libero-env:1 

outputs:
  stats_folder:
    type: uri_folder
    path: https://libero.blob.core.windows.net/libero/libero_converted

display_name: pi0_compute_norm_stats
experiment_name: compute_norm_stats
compute: nabk-E4ds
code: ../scripts/